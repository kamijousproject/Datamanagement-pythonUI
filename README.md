# 📱 Phone Data Manager

โปรแกรมจัดการข้อมูลเบอร์โทรศัพท์แบบครบวงจร พัฒนาด้วย Python และ Tkinter พร้อม SQLite Database

A comprehensive phone number data management application built with Python, Tkinter GUI, and SQLite database.

---

## ✨ Features / ฟีเจอร์หลัก

### 1. 📥 นำเข้าข้อมูล (Data Import)
- นำเข้าเบอร์โทรศัพท์จากไฟล์ .txt
- ตรวจสอบเบอร์ซ้ำในฐานข้อมูลอัตโนมัติ
- แยกเบอร์ซ้ำในไฟล์ไป Table 16 อัตโนมัติ
- แสดงตัวอย่างข้อมูลก่อนบันทึก
- รองรับ 16 ชุดข้อมูล (Tables 1-16)

### 2. 📊 จัดการข้อมูล (Data Management)
- ดูและค้นหาข้อมูลเบอร์โทรศัพท์
- กรองตามเบอร์, ชื่อชุดข้อมูล, และวันที่
- แสดงจำนวนเบอร์ทั้งหมดและเบอร์ซ้ำ
- ค้นหาข้อมูลแบบเรียลไทม์

### 3. 🔄 ย้ายข้อมูล (Data Transfer)
- ย้ายหรือคัดลอกข้อมูลระหว่าง Table
- เลือกชุดข้อมูลที่ต้องการย้าย
- ตัวเลือกลบข้อมูลต้นทางหลังย้าย
- แสดง Progress Bar ขณะย้ายข้อมูล

### 4. 📤 ส่งออกข้อมูล (Data Export)
- ส่งออกเบอร์เป็นไฟล์ .txt
- เลือกจำนวนเบอร์ที่ต้องการส่งออกจากแต่ละชุดข้อมูล
- ระบบติดตามเบอร์ที่ส่งออกแล้ว (is_exported flag)
- สุ่มแทรกเบอร์จากไฟล์อื่นเข้าไปด้วย (Optional)

### 5. 🔀 รวม 2 ไฟล์ (File Merge)
- รวมไฟล์ .txt 2 ไฟล์เข้าด้วยกัน
- สุ่มเรียงข้อมูลอัตโนมัติ
- บันทึกเป็นไฟล์ใหม่

### 6. 🔍 กรองเบอร์ซ้ำ (Duplicate Filter) **NEW!**
- ค้นหาและแสดงเบอร์ที่ซ้ำในแต่ละ Table
- แสดงจำนวนการซ้ำของแต่ละเบอร์
- ส่งออกรายงานเป็นไฟล์ CSV
- เรียงลำดับตามจำนวนการซ้ำ (มากไปน้อย)

---

## 🗄️ Database Structure / โครงสร้างฐานข้อมูล

**Database:** SQLite (`phone_data.db`)

**Tables:** `phone_data_set_1` ถึง `phone_data_set_16`

- **Table 1-15:** เก็บเบอร์ไม่ซ้ำ (ข้อมูลปกติ)
- **Table 16:** เก็บเบอร์ซ้ำในไฟล์ที่นำเข้า

**Schema:**
```sql
CREATE TABLE phone_data_set_X (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    phone_number TEXT,           -- เบอร์โทรศัพท์
    dataset_name TEXT,           -- ชื่อชุดข้อมูล
    receive_date TEXT,           -- วันที่รับข้อมูล
    source TEXT,                 -- แหล่งที่มา
    detail TEXT,                 -- รายละเอียด
    data_type TEXT,              -- ประเภทข้อมูล
    is_exported INTEGER DEFAULT 0,  -- สถานะการส่งออก
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

---

## 🚀 Installation / การติดตั้ง

### Option 1: ใช้ไฟล์ EXE (สำหรับผู้ใช้งานทั่วไป)

1. ดาวน์โหลดไฟล์ `PhoneDataManager.exe`
2. Double-click เพื่อรันโปรแกรม
3. ไม่ต้องติดตั้งอะไรเพิ่มเติม ✅

### Option 2: รันจาก Source Code (สำหรับนักพัฒนา)

1. **Clone repository**
```bash
git clone https://github.com/kamijousproject/Datamanagement-pythonUI.git
cd Datamanagement-pythonUI
```

2. **ติดตั้ง Python Dependencies**
```bash
pip install tkcalendar
```

3. **รันโปรแกรม**
```bash
python data-mange_SQLite.py
```

---

## 📋 Requirements / ความต้องการของระบบ

### สำหรับผู้ใช้ EXE:
- Windows 7/8/10/11 (64-bit)
- ไม่ต้องติดตั้ง Python

### สำหรับนักพัฒนา:
- Python 3.8 หรือสูงกว่า
- Required packages:
  ```
  tkinter (มากับ Python)
  tkcalendar
  sqlite3 (มากับ Python)
  ```

---

## 🛠️ Build EXE / การสร้างไฟล์ EXE

```bash
# ติดตั้ง PyInstaller
pip install pyinstaller

# Build เป็น EXE พร้อมไอคอน
pyinstaller --onefile --noconsole --name="PhoneDataManager" --icon="icon.ico" --hidden-import=tkcalendar --hidden-import=babel.numbers data-mange_SQLite.py
```

ไฟล์ EXE จะอยู่ที่ `dist/PhoneDataManager.exe`

---

## 📖 Usage Guide / คู่มือการใช้งาน

### 1. นำเข้าข้อมูล
1. ไปที่แท็บ "นำเข้าข้อมูล"
2. เลือกชุดข้อมูล (Table)
3. กรอกรายละเอียด (ชื่อชุดข้อมูล, วันที่, แหล่งที่มา)
4. เลือกไฟล์ .txt ที่มีเบอร์โทรศัพท์
5. ตรวจสอบตัวอย่างและกดบันทึก

### 2. จัดการข้อมูล
1. ไปที่แท็บ "จัดการข้อมูล"
2. เลือก Table ที่ต้องการดู
3. ใช้ฟิลเตอร์เพื่อค้นหาข้อมูล
4. ดูสถิติเบอร์ซ้ำด้านล่าง

### 3. ส่งออกข้อมูล
1. ไปที่แท็บ "ส่งออกข้อมูล"
2. เลือก Table และชุดข้อมูล
3. ระบุจำนวนเบอร์ที่ต้องการส่งออก
4. (Optional) เลือกไฟล์สำหรับสุ่มแทรกเบอร์
5. กดส่งออกและเลือกที่บันทึก

### 4. กรองเบอร์ซ้ำ
1. ไปที่แท็บ "กรองเบอร์ซ้ำ"
2. เลือก Table ที่ต้องการตรวจสอบ
3. กด "แสดงเบอร์ซ้ำ"
4. กด "ส่งออกเป็น CSV" เพื่อบันทึกรายงาน

---

## 📁 File Structure / โครงสร้างไฟล์

```
data-management-2/
├── data-mange_SQLite.py    # ไฟล์หลักของโปรแกรม
├── icon.ico                # ไอคอนโปรแกรม
├── phone_data.db          # ฐานข้อมูล SQLite (สร้างอัตโนมัติ)
├── .gitignore             # Git ignore file
├── README.md              # ไฟล์นี้
├── build/                 # โฟลเดอร์ build (ignore)
└── dist/                  # โฟลเดอร์ output EXE
    └── PhoneDataManager.exe
```

---

## 🔒 Data Security / ความปลอดภัยของข้อมูล

- ข้อมูลเก็บใน SQLite database แบบ local
- ไม่มีการส่งข้อมูลออกนอกเครื่อง
- สามารถ backup ไฟล์ `phone_data.db` ได้
- แนะนำให้เก็บไฟล์ database ไว้ในที่ปลอดภัย

---

## 🐛 Known Issues / ปัญหาที่ทราบ

- Font Kanit อาจแสดงผลไม่สมบูรณ์บนเครื่องที่ไม่มีฟอนต์ติดตั้ง (ไม่กระทบการใช้งาน)
- Warning `tzdata not found` จาก PyInstaller (ไม่กระทบการใช้งาน)

---

## 🤝 Contributing / การมีส่วนร่วม

1. Fork repository นี้
2. สร้าง Feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit การเปลี่ยนแปลง (`git commit -m 'Add some AmazingFeature'`)
4. Push ไปยัง Branch (`git push origin feature/AmazingFeature`)
5. เปิด Pull Request

---

## 📄 License / สัญญาอนุญาต

This project is open source and available for personal and commercial use.

---

## 👨‍💻 Author / ผู้พัฒนา

**Kamijous Project**
- GitHub: [@kamijousproject](https://github.com/kamijousproject)
- Repository: [Datamanagement-pythonUI](https://github.com/kamijousproject/Datamanagement-pythonUI)

---

## 📞 Support / การสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. เปิด Issue ใน GitHub Repository
2. ตรวจสอบ Known Issues ด้านบน
3. ตรวจสอบไฟล์ `phone_data.db` ว่าถูกสร้างในโฟลเดอร์เดียวกับโปรแกรม

---

## 🎉 Changelog / ประวัติการอัปเดต

### Version 2.1 (Latest)
- ✅ เพิ่ม Table 16 สำหรับเก็บเบอร์ซ้ำอัตโนมัติ
- ✅ ระบบแยกเบอร์ซ้ำในไฟล์อัตโนมัติ
- ✅ ปรับปรุงการนำเข้าข้อมูล

### Version 2.0
- ✅ เพิ่มฟีเจอร์ "กรองเบอร์ซ้ำ" พร้อมส่งออก CSV
- ✅ เพิ่ม custom icon
- ✅ ปรับปรุง UI/UX
- ✅ เพิ่ม .gitignore

### Version 1.0
- ✅ ฟีเจอร์พื้นฐาน: นำเข้า, จัดการ, ย้าย, ส่งออก
- ✅ รวม 2 ไฟล์พร้อมสุ่มเรียง
- ✅ SQLite Database

---

**Made with ❤️ for data management**
